FROM python:3.9-slim

WORKDIR /app

# Install only what the scripts need
COPY requirements.txt .
RUN pip install --no-cache-dir pandas yfinance kafka-python snowflake-connector-python python-dotenv

# Copy scripts and environment vars
COPY scripts/ ./scripts/
COPY .env .

CMD ["python", "scripts/producer.py"]